<%- include('partials/header') -%>
<body>
    <section class="bg-dark text-light p-5">
    <% if (locals.messages.errors) { %>
        <% messages.errors.forEach( el => { %>
            <div class="alert alert-danger"><%= el.msg %></div>
        <% }) %>    
    <% } %>
    <div class="container">
        <h1 class="text-light">Contact Us</h1>
    <form action="/contact/createContactNote" method="POST">
        <div class="row mb-3 form-group">
            <label for="contactName" class=" form-label">Name</label>
            <div class="col-sm-5">
                <input type="text" class="form-control" name="contactName">
            </div>
            </div>
        <div class="row mb-3 form-group">
            <label for="contactEmail" class=" form-label">Email</label>
            <div class="col-sm-5">
                <input type="email" class="form-control" name="contactEmail">

            </div>
        </div>
        <div class="row mb-3 form-group">
            <label for="contactType" class=" form-label">Choose Category</label>
            <div class="col-sm-5"> 
            <select class="form-control" id="contactType">
                    <option>Question</option>
                    <option>Comment</option>
                    <option>Idea</option>
                    <option>Suggestion</option>
                  </select>
                </div>
        </div>
        <div class="row mb-3 form-group">
            <label for="contactNote" class=" form-label">Message</label>    
         <div class="col-sm-10">   
            <textarea class="form-control" name="contactNote" rows="3"></textarea>
        </div>
    </div>
        <button type="submit" class="btn btn-success">Submit</button>
    </form>
</div>
</section>
<section class="bg-light text-dark p-5">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-md">
              <img src="/images/undraw_hello_re_3evm.svg" class="img-fluid" alt="People tasting food and rating it">
            </div>
            <div class="col-md p-5">
              <h2>Thanks for reaching out!</h2>
              <p class="lead">
               We appreciate hearing your comments and feedback.
              </p>
            </div>
          </div>
    </div>
</section>
</body>
</html>
<%- include('partials/footer') -%> 